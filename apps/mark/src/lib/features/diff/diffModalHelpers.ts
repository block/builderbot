// Re-export from shared package
export {
  buildFileEntries,
  buildTree,
  compactTree,
  formatLineRange,
  truncateText,
  type FileEntry,
  type TreeNode,
} from '@builderbot/diff-viewer/utils';
