skip_lfs: true
output:
  - success
  - failure

pre-push:
  parallel: true
  commands:
    staged-fmt-check:
      root: "staged/"
      run: just fmt-check
    staged-lint:
      root: "staged/"
      run: just lint
    staged-typecheck:
      root: "staged/"
      run: just typecheck
